apiVersion: v1
kind: Service
metadata:
  name: {{ include "dictator.fullname" . }}
  labels:
    {{- include "dictator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dictator.selectorLabels" . | nindent 4 }}
