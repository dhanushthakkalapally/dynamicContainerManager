apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamicContainerManager.fullname" . }}
  labels:
    {{- include "dynamicContainerManager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dynamicContainerManager.selectorLabels" . | nindent 4 }}
